# 第二章：理解 Agent 框架

## 1. 先别急着背定义

教材里的定义是：

> AI 代理框架是为简化 AI 代理的创建、部署和管理而设计的软件平台。

这句话没有错，但太像“说明书语言”了。更直白一点：

**Agent 框架不是让模型更会说话，而是让模型更会做事。**

它解决的不是“怎么调用一次大模型”，而是下面这些更实际的问题：

- 用户的目标怎么保存和理解？
- 当前对话里的关键信息怎么管理？
- 什么时候该查资料、调接口、执行代码？
- 多步任务先做什么、后做什么？
- 做错了怎么发现，怎么修正？
- 用户下次再来时，哪些信息需要记住？

所以，Agent 框架本质上是一套“让 AI 稳定完成任务”的运行系统。

---

## 2. 一个 Agent 真正在做什么

如果只看表面，你会觉得 agent 像是在“聊天”。

但从系统运行角度看，一个 agent 通常在做下面这件事：

```text
接收目标
-> 读取上下文
-> 判断当前缺什么信息
-> 制定下一步计划
-> 调用工具执行
-> 观察结果
-> 根据反馈修正
-> 更新记忆
-> 继续下一轮
```

也就是说，agent 不是“回答一次问题”就结束，而是围绕目标不断循环。

这也是为什么学习 Agent 框架时，最重要的不是背框架名字，而是理解它的五个核心组成部分：

1. 上下文
2. 工具
3. 记忆
4. 规划
5. 反馈闭环

---

## 3. 五个核心部分

### 3.1 上下文：Agent 此刻正在看的信息

#### 什么是上下文

上下文就是 agent 在当前这一轮决策中会参考的信息。

比如：

- 用户刚刚说了什么
- 这次任务的目标是什么
- 系统提示词是什么
- 前面几轮对话发生了什么
- 刚刚某个工具返回了什么结果

#### 上下文的特点

- **短期性**：它主要服务于“当前这一步该怎么做”。
- **动态性**：随着对话和任务推进，上下文会不断变化。
- **容量有限**：不是所有信息都能无限塞进去。
- **强影响决策**：模型最后怎么回答、要不要调用工具，很大程度取决于它看到了什么上下文。

#### 你可以把它想成什么

上下文像是 agent 当前桌面上摊开的资料。

桌面上放着：

- 当前任务说明
- 用户最近几轮消息
- 工具刚刚返回的数据
- 系统对它的行为约束

agent 就根据桌上的这些资料来决定下一步。

#### 常见问题

- 上下文过多：模型抓不住重点，回答会发散。
- 上下文过少：模型信息不够，容易瞎猜。
- 上下文组织混乱：模型不知道哪些信息最重要。

#### 一句话理解

**上下文决定 agent“此刻怎么想”。**

---

### 3.2 工具：Agent 与外部世界交互的手和脚

#### 什么是工具

工具是 agent 可以调用的外部能力。

大模型本身只能生成文本，但现实任务通常不只是“说”，还要“做”。这时就需要工具。

常见工具包括：

- 搜索网页
- 查询数据库
- 调用 API
- 执行 Python 或 Shell 代码
- 发送邮件或消息
- 读写文件
- 调用企业内部系统

#### 工具的特点

- **可执行性**：工具不是解释世界，而是直接对世界产生操作。
- **输入输出清晰**：一个工具通常有明确参数和返回结果。
- **能力边界明确**：每个工具只负责某一类事情。
- **需要权限控制**：工具越强，风险越高。

#### 为什么工具重要

没有工具，agent 往往只能：

- 给建议
- 生成文本
- 假装知道一些事实

有了工具，agent 才能真正：

- 去查
- 去算
- 去执行
- 去验证

#### 常见误区

- 误以为 agent 自己“知道”实时信息，其实很多时候是工具帮它查到的。
- 误以为工具越多越好，其实工具太多会让决策变复杂。
- 误以为注册工具就够了，实际上还要考虑错误处理、超时、权限、重试。

#### 一句话理解

**工具决定 agent“此刻能做什么”。**

---

### 3.3 记忆：Agent 长期保留的信息

#### 什么是记忆

记忆是 agent 在当前会话之外，还希望长期保留的信息。

比如：

- 用户偏好
- 历史任务结果
- 经常使用的知识片段
- 之前总结过的事实
- 某类任务中积累的经验

#### 记忆的特点

- **长期性**：它不只服务当前一轮，而是服务未来。
- **可检索性**：通常不会整包塞进上下文，而是在需要时取出来。
- **选择性**：不是所有内容都值得记住。
- **结构化程度不同**：可以是简单字段，也可以是向量化知识片段。

#### 上下文和记忆的区别

这是最容易混淆的地方。

- **上下文**：当前要用的信息。
- **记忆**：未来可能还要用的信息。

可以这样类比：

- 上下文像工作台
- 记忆像档案柜

工作台上的资料是当前摊开的；
档案柜里的资料平时不看，需要时再取。

#### 为什么记忆重要

没有记忆，agent 每次都像第一次见你。

有了记忆，它才可能表现出下面这些能力：

- 记住用户偏好
- 延续长期任务
- 在多轮交互中保持一致性
- 避免重复询问同样的问题

#### 记忆的难点

- 记什么，不记什么
- 什么时候更新
- 如何防止记错
- 如何避免把旧信息当成新事实

#### 一句话理解

**记忆决定 agent“以后还记得什么”。**

---

### 3.4 规划：Agent 把目标拆成步骤的能力

#### 什么是规划

规划就是把一个目标拆成可执行的步骤，并决定先做什么、后做什么。

例如用户说：

> 帮我调研三种 agent 框架，并给我一页总结。

如果没有规划，模型可能立刻开始乱写。

如果有规划，它更可能按这样的顺序做：

1. 识别任务目标和输出形式
2. 确定要比较的框架
3. 决定比较维度
4. 搜集资料
5. 归纳结论
6. 生成总结页

#### 规划的特点

- **面向目标**：它关心的是“怎样到达目标”。
- **分步骤**：它把复杂任务拆成多个小动作。
- **可调整**：如果中途发现信息不够，计划会修改。
- **可显式也可隐式**：有时系统会让模型写出计划，有时只是内部完成。

#### 规划为什么重要

很多任务不是一句话能完成的，比如：

- 写报告
- 做调研
- 预订行程
- 分析数据
- 多代理协作

这些任务都需要顺序、依赖关系和阶段判断。

#### 规划和工具的区别

- **规划**回答的是：下一步应该做什么。
- **工具**回答的是：要做这一步时，用什么去做。

所以：

- 规划是路线图
- 工具是执行手段

#### 一句话理解

**规划决定 agent“接下来该怎么走”。**

---

### 3.5 反馈闭环：让 Agent 不断修正自己的机制

#### 什么是反馈闭环

反馈闭环指的是：agent 做完一步后，不是立刻结束，而是会检查结果，再决定下一步。

反馈可以来自：

- 用户反馈
- 工具执行结果
- 环境变化
- 系统规则
- 自我检查结果

#### 反馈闭环的特点

- **可迭代**：不是一次生成，而是多轮修正。
- **可纠错**：发现问题后能重试或改计划。
- **可优化**：长期看会逐步提升效果。
- **更接近真实任务流程**：现实工作几乎都不是一步到位。

#### 举个例子

用户说：“帮我整理一份会议纪要。”

agent 可能会这样循环：

1. 先提取要点
2. 生成初稿
3. 检查是否漏掉行动项
4. 发现缺少负责人
5. 重新读取原文或追问用户
6. 更新纪要

这就是反馈闭环。

#### 为什么它很关键

没有反馈闭环，agent 常常是“一次输出定生死”。

有了反馈闭环，它才更像一个真正的执行者：

- 会检查
- 会回头
- 会修正
- 会越来越稳

#### 一句话理解

**反馈闭环决定 agent“做得不对时会不会改”。**

---

## 4. 五个部分之间是什么关系

这五部分不是并列的概念堆砌，它们在运行时是连在一起的。

可以用下面这张图理解：

```text
用户目标
   ↓
上下文：现在有哪些信息可用？
   ↓
规划：下一步应该做什么？
   ↓
工具：要如何执行这一步？
   ↓
结果返回
   ↓
反馈闭环：结果够不够好？要不要修正？
   ↓
记忆：哪些内容值得长期保留？
   ↓
进入下一轮
```

如果只用一句话概括：

- 上下文负责“看见现在”
- 工具负责“作用现实”
- 记忆负责“连接过去和未来”
- 规划负责“组织步骤”
- 反馈闭环负责“持续修正”

---

## 5. 用一个完整例子把它串起来

假设你做一个“调研助手” agent，用户说：

> 帮我比较 AutoGen、Semantic Kernel 和 Azure AI Agent Service，并写一份初学者能看懂的总结。

### 这个 agent 会怎么工作

| 阶段 | Agent 在做什么 | 对应能力 |
| --- | --- | --- |
| 1 | 读取用户要求，识别输出目标是“比较 + 总结” | 上下文 |
| 2 | 判断需要先收集三个框架的信息，再提炼维度 | 规划 |
| 3 | 调用搜索、文档读取或本地资料工具 | 工具 |
| 4 | 把查到的内容整理进当前任务信息里 | 上下文 |
| 5 | 如果发现资料不够一致，重新补查 | 反馈闭环 |
| 6 | 记住用户偏好“要中文、要易懂、面向初学者” | 记忆 |
| 7 | 输出最终总结，并在下次类似任务中复用这些偏好 | 记忆 + 规划 |

### 这里最重要的观察

你会发现，agent 的能力不是来自某一个神奇模块，而是来自这五部分的配合。

少了其中任何一个，系统都会明显变弱：

- 没有上下文：不知道当前任务是什么
- 没有工具：只能说，不能做
- 没有记忆：每次都像第一次见你
- 没有规划：复杂任务容易乱
- 没有反馈闭环：做错了也不会改

---

## 6. Agent 框架到底帮开发者做了什么

理解了上面的五部分，再回头看“框架”的价值就清楚了。

Agent 框架主要是在帮开发者把这些能力标准化：

- 管理上下文，而不是手动拼接所有 prompt
- 注册和调用工具，而不是自己写一套混乱的函数路由
- 存取记忆，而不是每次都从零开始
- 编排多步任务和多代理协作
- 建立反馈、日志、追踪、重试和可观测性

所以框架真正提供的，不只是几个 API，而是一套运行秩序。

---

## 7. 结合本章提到的框架来理解

这一章提到的几个框架，可以放进上面的主线里看。

### Semantic Kernel 更像什么

它更强调把模型、插件、提示模板、记忆和编排能力组织起来。

你可以把它理解成：

- 比较适合把“上下文、工具、记忆”管理得更清楚
- 很适合企业应用接入插件和服务
- 很适合把大模型能力接进现有系统

### AutoGen 更像什么

它更强调多 agent 协作、消息传递和任务编排。

你可以把它理解成：

- 比较适合突出“规划、协作、反馈闭环”
- 很适合多角色分工，比如检索 agent、分析 agent、审核 agent
- 很适合研究型和实验型 agent 工作流

### Azure AI Agent Service 更像什么

它更强调托管式运行、线程、工具集成和企业级部署。

你可以把它理解成：

- 不只是一个 SDK，更像 agent 的运行平台
- 让上下文、线程、工具接入、托管部署更完整
- 更适合想把 agent 放进真实业务环境里

### 重点不是死记差异

不管框架名字怎么变，它们都绕不开同一件事：

**如何让 agent 在真实任务里，稳定地管理上下文、调用工具、保留记忆、进行规划，并通过反馈不断修正。**

---

## 8. 学完这章后，你应该真正理解什么

如果你已经能回答下面这些问题，就说明这章你真的掌握了：

- Agent 框架和普通“调用 LLM”有什么区别？
- 为什么一个 agent 不能只有 prompt，还需要工具和状态管理？
- 上下文和记忆到底差在哪里？
- 为什么复杂任务离不开规划？
- 为什么没有反馈闭环的 agent 往往不够可靠？
- 框架的价值为什么不只是“封装 API”？

---

## 9. 一段总结

Agent 框架的核心，不是把大模型包得更漂亮，而是让它从“会生成文本”变成“能围绕目标持续行动”。

而这个能力，几乎都可以落到五个问题上：

- 现在知道什么？这是上下文。
- 现在能做什么？这是工具。
- 以后还记得什么？这是记忆。
- 接下来怎么走？这是规划。
- 做错了会不会改？这是反馈闭环。

把这五件事看懂了，再去看任何 agent 框架，你都会更容易抓住本质。
